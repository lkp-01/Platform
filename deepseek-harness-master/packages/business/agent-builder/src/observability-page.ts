/** Governed portal presentation; all data comes from the same authorized analytics API. */
export const observabilityScript = String.raw`
Object.assign(words.zh,{rejected:'派发前拒绝',sort:'排序',failures:'失败次数',share:'失败占比',delta:'成功率变化（百分点）',analytics:'运行分析',workspaceAnalytics:'工作区运行总览',window:'统计范围',day:'过去 24 小时',week:'过去 7 天',month:'过去 30 天',last1000:'最近 1000 次终结运行',custom:'自定义时间',from:'开始时间 (UTC)',to:'结束时间 (UTC)',analyze:'分析',samples:'样本数',successRate:'运行成功率',cancelled:'取消',latency:'平均耗时 / P95 (ms)',tokenAverage:'平均 Token',coverage:'完整 Token 样本',toolSuccess:'工具尝试成功率',logicalSuccess:'工具最终成功率',timeouts:'超时率',retryCount:'额外尝试',intervention:'人工介入运行',cost:'已知估算成本',unpriced:'未定价模型调用',analysisHint:'运行成功不代表答案正确。取消与未知数据单列；成本为估算。',dataPartial:'数据尚未完整，请稍后刷新',dataComplete:'投影已同步',indexed:'已建立索引 / 全部运行',activeRuns:'当前在途运行',unknownTime:'结束时间未知',failureReasons:'任务失败原因',callErrors:'调用失败原因',affected:'受影响 Agent / Run',dependencies:'依赖健康',compare:'对比版本',baseline:'基准版本',lowSample:'样本较少（少于 30），不代表统计显著',trend:'每日趋势',drill:'查看匹配运行',noValue:'未知',costCoverage:'已定价调用',analysisAgent:'Agent 排行',attempts:'调用次数',resourceVersionLabel:'资源版本',callSuccess:'调用成功率',finalSuccess:'最终成功率'});
Object.assign(words.en,{rejected:'Rejected before dispatch',sort:'Sort',failures:'Failure count',share:'Share of failures',delta:'Success change (percentage points)',analytics:'Analytics',workspaceAnalytics:'Workspace observability',window:'Window',day:'Last 24 hours',week:'Last 7 days',month:'Last 30 days',last1000:'Last 1000 terminal runs',custom:'Custom time',from:'From (UTC)',to:'To (UTC)',analyze:'Analyze',samples:'Samples',successRate:'Task success rate',cancelled:'Cancelled',latency:'Average / P95 latency (ms)',tokenAverage:'Average tokens',coverage:'Complete token samples',toolSuccess:'Tool attempt success',logicalSuccess:'Tool final success',timeouts:'Timeout rate',retryCount:'Extra attempts',intervention:'Runs needing intervention',cost:'Known estimated cost',unpriced:'Unpriced model calls',analysisHint:'Execution success does not imply answer correctness. Cancellations and unknown data are separate; costs are estimates.',dataPartial:'Data is incomplete. Refresh shortly.',dataComplete:'Projection synchronized',indexed:'Indexed / eligible runs',activeRuns:'Current active runs',unknownTime:'Unknown finish time',failureReasons:'Task failure reasons',callErrors:'Call failure reasons',affected:'Affected Agents / Runs',dependencies:'Dependency health',compare:'Comparison version',baseline:'Baseline version',lowSample:'Small sample (under 30); no significance claim',trend:'Daily trend',drill:'Matching runs',noValue:'Unknown',costCoverage:'Priced calls',analysisAgent:'Agent ranking',attempts:'Calls',resourceVersionLabel:'Resource version',callSuccess:'Attempt success',finalSuccess:'Final success'});
const metric=(v,percent=false)=>v===null||v===undefined?t('noValue'):percent?(v*100).toFixed(1)+'%':Number(v).toLocaleString(undefined,{maximumFractionDigits:2});
function analysisTable(parent,heads,rows){const table=el('table');const head=el('tr');for(const title of heads)head.append(el('th',title));const thead=el('thead');thead.append(head);table.append(thead);const body=el('tbody');for(const values of rows){const row=el('tr');for(const value of values){const cell=el('td');if(value instanceof Node)cell.append(value);else cell.textContent=String(value);row.append(cell);}body.append(row);}table.append(body);const container=el('div',undefined,'analysis-table');container.append(table);parent.append(container);}
function analysisCards(parent,s){const grid=el('div',undefined,'grid');for(const [key,value] of [['samples',s.sampleCount],['successRate',metric(s.successRate,true)],['cancelled',s.cancelled],['latency',metric(s.latency.average)+' / '+metric(s.latency.p95)],['tokenAverage',metric(s.tokens.average)],['coverage',s.tokens.validCount+' / '+s.sampleCount],['toolSuccess',metric(s.tools.successRate,true)],['retryCount',s.tools.retries+s.models.retries],['intervention',s.interventionRuns],['unpriced',s.unpricedCalls],['rejected',s.rejectedCalls]]){const card=el('div',undefined,'card');card.append(el('span',t(key),'muted'),el('h2',value));grid.append(card);}parent.append(grid);for(const cost of s.costs)parent.append(el('p',t('cost')+': '+cost.currency+' '+(Number(cost.nanoUnits)/1e9).toFixed(6)+' · '+t('costCoverage')+': '+cost.pricedCalls));}
async function analytics(a,query){shell();app.append(el('h2',a?a.name+' · '+t('analytics'):t('workspaceAnalytics')),el('p',t('analysisHint'),'muted'));const form=el('div',undefined,'card');const range=select(form,t('window'),[['week',t('week')],['day',t('day')],['month',t('month')],['last1000',t('last1000')],['custom',t('custom')]],query?.window.kind==='last'?'last1000':'week');const from=field(form,t('from'),query?.window.from||new Date(Date.now()-7*86400000).toISOString());const to=field(form,t('to'),query?.window.to||new Date().toISOString());let baseline,comparison;if(a){let versions=[],cursor=0;do{const page=await rpc('versions',a.id,cursor);versions.push(...page.items);cursor=page.nextCursor;}while(cursor!==null);const choices=[['',t('all')],...versions.map(v=>[v.id,'v'+v.versionNumber])];baseline=select(form,t('baseline'),choices,query?.versionId||'');comparison=select(form,t('compare'),choices,query?.compareVersionId||'');}form.append(button(t('analyze'),()=>{const now=Date.now();const window=range.value==='last1000'?{kind:'last',count:1000}:range.value==='custom'?{kind:'time',from:new Date(from.value).toISOString(),to:new Date(to.value).toISOString()}:{kind:'time',from:new Date(now-({day:1,week:7,month:30}[range.value])*86400000).toISOString(),to:new Date(now).toISOString()};return analytics(a,{window,...(a?{agentId:a.id}:{}),...(baseline?.value?{versionId:baseline.value}:{}),...(comparison?.value?{compareVersionId:comparison.value}:{})});},true));app.append(form);query=query||{window:{kind:'time',from:from.value,to:to.value},...(a?{agentId:a.id}:{})};const report=await rpc('observability',query);app.append(el('p',t(report.dataState==='complete'?'dataComplete':'dataPartial')+' · '+t('indexed')+': '+report.indexedRunCount+' / '+report.eligibleRunCount,'muted'));analysisCards(app,report.summary);app.append(el('p',t('activeRuns')+': '+report.active.map(r=>r.status+' '+r.count).join(' · ')+' · '+t('unknownTime')+': '+report.unknownTimeCount));if(report.comparison){app.append(el('h3',t('compare')+' · '+report.comparison.versionId));if(report.comparison.lowSample)app.append(el('p',t('lowSample'),'muted'));analysisCards(app,report.comparison.summary);if(report.summary.successRate!==null&&report.comparison.summary.successRate!==null)app.append(el('p',t('delta')+': '+((report.comparison.summary.successRate-report.summary.successRate)*100).toFixed(1)));}app.append(button(t('drill'),()=>analysisRuns(a,query,report.revision)));app.append(el('h3',t('failureReasons')));analysisTable(app,[t('kind'),t('samples'),t('share'),t('drill')],report.summary.errors.map(e=>[e.category,e.count,metric(e.count/report.summary.failed,true),button(t('drill'),()=>analysisRuns(a,{...query,errorCategory:e.category},report.revision))]));app.append(el('h3',t('dependencies')));const sorter=select(app,t('sort'),[['failures',t('failures')],['timeouts',t('timeouts')],['latency',t('latency')],...report.summary.costs.map(c=>['cost:'+c.currency,t('cost')+' · '+c.currency])]);const dependencies=el('div');app.append(dependencies);function renderDependencies(){dependencies.replaceChildren();const value=r=>sorter.value==='timeouts'?r.calls.timeoutRate??-1:sorter.value==='latency'?r.calls.latency.p95??-1:sorter.value.startsWith('cost:')?Number(r.costs.find(c=>c.currency===sorter.value.slice(5))?.nanoUnits??-1):r.calls.failed;const ordered=[...report.resources].sort((a,b)=>value(b)-value(a));analysisTable(dependencies,[t('name'),t('resourceVersionLabel'),t('attempts'),t('callSuccess'),t('finalSuccess'),t('timeouts'),t('latency'),t('affected'),t('cost'),t('callErrors'),t('drill')],ordered.map(r=>[r.name,r.resourceVersionId?r.resourceVersionId.slice(-8):t('noValue'),r.calls.count,metric(r.calls.successRate,true),metric(r.calls.logicalSuccessRate,true),metric(r.calls.timeoutRate,true),metric(r.calls.latency.average)+' / '+metric(r.calls.latency.p95),r.affectedAgents+' / '+r.affectedRuns,r.costs.map(c=>c.currency+' '+(Number(c.nanoUnits)/1e9).toFixed(6)).join(', ')||t('noValue'),r.errors.map(e=>e.category+': '+e.count).join(', '),button(t('drill'),()=>analysisRuns(a,{...query,resourceKey:r.key},report.revision))]));}sorter.onchange=renderDependencies;renderDependencies();app.append(el('h3',t('trend')));analysisTable(app,[t('window'),t('samples'),t('successRate'),t('latency')],report.trend.map(row=>[row.date,row.summary.sampleCount,metric(row.summary.successRate,true),metric(row.summary.latency.average)+' / '+metric(row.summary.latency.p95)]));if(!a){app.append(el('h3',t('analysisAgent')));analysisTable(app,[t('agents'),t('samples'),t('successRate'),t('drill')],report.agents.map(row=>[row.agentId,row.summary.sampleCount,metric(row.summary.successRate,true),button(t('analytics'),async()=>analytics(await rpc('agent',row.agentId)))]));}}
async function analysisRuns(a,query,revision,cursor){const page=await rpc('observabilityRuns',query,...(cursor?[cursor]:[]));if(page.revision!==revision){await analytics(a,query);return;}shell();app.append(el('h2',t('drill')),button(t('back'),()=>analytics(a,query)));for(const row of page.items){const card=el('div',undefined,'card');card.append(el('p',row.runId+' · '+row.status),button(t('trace'),()=>runDetail({id:row.agentId,name:row.agentId},row.runId)));app.append(card);}if(!page.items.length)app.append(el('p',t('empty')));if(page.nextCursor)app.append(button(t('more'),()=>analysisRuns(a,query,revision,page.nextCursor)));}
`
